version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --cache .npm --loglevel=error --progress=false
        - echo "Note: amplify_outputs.json will be created from SSM parameters or environment"
    build:
      commands:
        - CI=false NODE_ENV=production npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*